---
description: Steps to initialize a git repository and push to GitHub
---

1. Initialize git repository
2. Add all files
3. Commit changes with message "Initial commit: DermAnalysis v2.0 - Supabase & Decision Engine"
4. Create a new public repository on GitHub called `derm-analysis-app` using `gh repo create`
5. Push changes to origin main
